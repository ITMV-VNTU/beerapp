extends layout

block content
  div
      each item,i in data
        - var colapse="colapse"+i;
            div
              h1 #{item.name}
              h4 #{item.shortName}
              p #{item.description}
