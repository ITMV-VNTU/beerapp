extends layout

block content
    form(action="/addComment?id="+id, method="POST",id='form_container',enctype="application/x-www-form-urlencoded")
        div(class="form-group")
            label(for="usr") Name
            input(type="text" class="form-control" name="name" id="user")
            label(for="comment") Comment:
            textarea(class="form-control" name="comment" form="form_container" rows="5" id="comment")
            button(type="submit" class="btn btn-default") Submit